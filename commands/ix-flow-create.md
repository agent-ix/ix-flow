---
description: Author a new Agent IX workflow (a flow plus the skill that runs it).
argument-hint: "[name]"
---

Author a new Agent IX workflow using the **ix-flow-create** skill. Use `$ARGUMENTS` as the
workflow name if provided; otherwise ask for one. Scaffold the flow (`def.yaml`) and the
skill (`SKILL.md`) that runs it, then smoke-test the result.

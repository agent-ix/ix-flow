name: CI

# Manual only. Never on push, pull request, commit, or merge.
on:
  workflow_dispatch:
jobs:
  ci:
    uses: agent-ix/nodejs-actions/.github/workflows/build-test.yml@main
    with:
      npm_registry: https://registry.npmjs.org
      trusted_publish: true

# FastAPI Service Spec for ${instance.id}

This artifact uses the **fastapi-service** variant template. Service
specs include API endpoint definitions, request/response schemas, and
operational NFRs around request latency and throughput.

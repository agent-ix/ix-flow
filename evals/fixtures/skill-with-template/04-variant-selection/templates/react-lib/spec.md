# React Library Spec for ${instance.id}

This artifact uses the **react-lib** variant template. Library specs
describe public component APIs, Storybook coverage, and bundling
constraints rather than service endpoints.

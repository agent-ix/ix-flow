# Notes for ${instance.id}

Generated at ${now} in phase **${instance.phase}**.

This is a minimal templating example. The engine substituted three
variables: the instance id, the timestamp, and the current phase.
